# Core Data Science
numpy>=1.24.0,<2.0
scipy>=1.10.0,<1.14
pandas>=2.0.0
scikit-learn>=1.2.0

# NLP / ML -- real embeddings, no fallback
# torch 2.2.2 is the latest for Python 3.11 macOS x86_64
torch>=2.0.0,<=2.2.2
# sentence-transformers <4 is compatible with torch 2.2
sentence-transformers>=2.2.2,<4.0.0
rank-bm25>=0.2.2

# FastAPI Backend (demo only, NOT on rank path)
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
openai>=1.0.0
openai>=1.0.0

# Qdrant (for UI demo search)
qdrant-client>=1.4.0

# Utilities
tqdm>=4.65.0
PyYAML>=6.0
python-multipart>=0.0.6
openpyxl>=3.1.0
pandas>=2.0.0
